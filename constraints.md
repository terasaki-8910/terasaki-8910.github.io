GitHub Pagesは非常に便利なサービスですが、静的サイトホスティングならではのいくつかの重要な制約があります。

ご質問の項目に沿って、主な制約を以下にまとめます。

### 1. 使用不可言語・コンポーネント

GitHub Pagesは**静的サイト**のホスティングサービスです。つまり、サーバー側でプログラムを実行する機能はありません。

* **使用不可（サーバーサイド言語）**
    * **Ruby (Ruby on Rails)**
    * **PHP**
    * **Python** (Django, Flaskなど)
    * **Node.js** (Expressなど)
    * **Java** (Springなど)
    * **データベース** (MySQL, PostgreSQL, MongoDBなど)

* **使用可能（クライアントサイド言語）**
    * **HTML**
    * **CSS**
    * **JavaScript**

**補足:**
「Ruby (Jekyll) が使える」と聞いたことがあるかもしれませんが、これはJekyllがGitHub Pagesのサーバー上で実行されるのではなく、Jekyllというツールが`push`のタイミングで（またはGitHub Actionsで）**静的なHTML/CSS/JSファイル群を生成（ビルド）する**ために使われる、という意味です。Ruby on Railsのように、ユーザーからのアクセスがあるたびにRubyコードが動的に実行されるわけではありません。

### 2. 音声出力 (Audio Output)

**特に制約はありません。**

`.mp3`や`.wav`、`.ogg`といった音声ファイルは、画像やCSSファイルと全く同じ「静的なファイル」として扱われます。
HTMLの`<audio>`タグなどを使えば、ブラウザ側で問題なく再生（音声出力）が可能です。

ただし、後述する「最大容量」の制約にはカウントされます。

### 3. 最大容量 (Capacity Limits)

容量と転送量には、以下の公式な制限（または推奨値）があります。

* **公開されるサイトの最大サイズ: 1 GB**
    * リポジトリのサイズではなく、最終的にビルドされて公開されるサイト（`_site`フォルダの中身）の合計サイズです。

* **リポジトリの推奨サイズ: 1 GB**
    * GitHub Pagesの元となるリポジトリのサイズは、1GB未満に抑えることが推奨されています。

* **帯域幅 (データ転送量): 月間 100 GB**
    * これは「ソフトリミット（緩やかな制限）」です。
    * 1ヶ月に100GBを超えるアクセスがあった場合、サイトの表示が制限されたり、GitHubからトラフィックを削減するよう（例：CDNサービスの利用を推奨する）連絡が来たりする可能性があります。

* **個別ファイルの最大サイズ: 100 MB**
    * Gitリポジトリへの`push`は、1ファイルあたり100MBがハードリミット（上限）です。
    * 50MBを超えるファイルを`push`しようとすると警告が表示されます。

### 4. パスの深さ (Path Depth)

**「ディレクトリの階層の深さ（例: 10階層まで）」という明確な制限はありません。**

ただし、関連する制限として「**パスの長さ**」の制約があります。

これはGitHub Pages固有の制限ではなく、GitやOS（特にWindows）のファイルシステムに起因するものです。ファイル名を含めたパス全体の長さ（例: `folder1/folder2/folder3/my-file.html` の文字数）が**約260文字**を超えると、OSやツールによってはファイルを正しく扱えなくなる可能性があります。

常識的な範囲（数階層程度）のディレクトリ構造であれば、問題になることはありません。

### 5. その他の主な制約

* **ビルド回数: 1時間あたり10回**
    * Jekyllを使ってサイトをビルドする場合（`push`するたびに自動でサイトが更新される場合）、そのビルドは1時間に10回までというソフトリミットがあります。（※GitHub Actionsを使って自分でビルド・デプロイ処理を組む場合は、この制限の対象外となります）

* **商用利用の制限**
    * GitHub Pagesは、オンラインビジネスやEコマースサイト、SaaS（サービスとしてのソフトウェア）の提供など、**主要な目的が商取引であるサイトの運営には使用できません**。
    * プロジェクトのドキュメント、ポートフォリオ、情報発信ブログなどでの利用が想定されています。